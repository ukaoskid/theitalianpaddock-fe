type TMetric = 'speed' | 'throttle' | 'brake' | 'ngear' | 'rpm' | 'drs';
type TSession = 'Q' | 'R';
type TComboData = { text: string; value: string | number; }

export type { TMetric, TSession, TComboData }
